## ShopCRM BD (Frontend)

### Setup

```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

The app expects the backend at `NEXT_PUBLIC_API_BASE_URL` (defaults to `http://127.0.0.1:8000/api/v1/`).

